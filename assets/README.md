# Assets

`thumbnail.png` is the default editable thumbnail template. Replace it with your own 1280x720 PNG/JPG if desired.

PIP assets are intentionally removed: PIP is already embedded in the Drive video library clips.
